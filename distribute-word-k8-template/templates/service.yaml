apiVersion: v1
kind: Service
metadata:
  name: {{ include "distribute-word-k8-template.fullname" . }}
  labels:
    {{- include "distribute-word-k8-template.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "distribute-word-k8-template.selectorLabels" . | nindent 4 }}
